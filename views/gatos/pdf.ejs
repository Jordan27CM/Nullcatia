<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Lista de Gatitos</title>
  <style>
    body {
      font-family: Georgia, serif;
      font-size: 14px;
      margin: 40px;
      background-color: #2e2a1f;
      color: #f8f4dc;
    }

    h1 {
      text-align: center;
      font-size: 28px;
      margin-bottom: 30px;
      color: #d9c978;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background-color: #3b3721;
    }

    th, td {
      border: 1px solid #d9c978;
      padding: 10px;
      text-align: center;
    }

    th {
      background-color: #4c4728;
      color: #d9c978;
      font-size: 16px;
    }

    td {
      font-size: 14px;
    }

    tr:nth-child(even) {
      background-color: #2e2a1f;
    }

    .footer {
      margin-top: 50px;
      text-align: center;
      font-size: 12px;
      color: #aaa;
    }
  </style>
</head>
<body>
  <h1>🐾 Lista de Gatitos del Reino</h1>

  <table>
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Edad</th>
        <th>Raza</th>
        <th>Clan</th>
      </tr>
    </thead>
    <tbody>
      <% gatos.forEach(g => { %>
        <tr>
          <td><%= g.nombre %></td>
          <td><%= g.edad %></td>
          <td><%= g.raza %></td>
          <td><%= g.clan_nombre %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>

  <div class="footer">
    Generado automáticamente por el sistema de NULLCATIA · <%= new Date().toLocaleDateString() %>
  </div>
</body>
</html>
